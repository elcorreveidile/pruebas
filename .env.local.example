# === Base de datos (Prisma / Neon) ===
# Neon → Connection string (incluye ?sslmode=require). Postgres local también vale.
DATABASE_URL="postgresql://user:password@host/db?sslmode=require"

# === Sesión (auth magic link) ===
# Genera uno con: openssl rand -base64 32
SESSION_SECRET=""

# === Email transaccional (Brevo) ===
BREVO_API_KEY=""
BREVO_SENDER_EMAIL="no-responder@lafabricadesonrisasgranada.com"
BREVO_SENDER_NAME="La Fábrica de Sonrisas"
LEADS_NOTIFY_EMAIL="lafabricadesonrisasgranada@gmail.com"

# === Panel ===
ADMIN_EMAIL="javier@blablaele.com"

# === Sitio ===
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
NEXT_PUBLIC_WHATSAPP_NUMBER="34663828930"

# === Medios (Vercel Blob, solo producción) ===
BLOB_READ_WRITE_TOKEN=""
